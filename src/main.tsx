import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(
  document.getElementById('root')!
).render(
  <React.StrictMode>
    <div
      style={{
        background: 'white',
        color: 'black',
        padding: '50px',
        fontSize: '32px',
      }}
    >
      REACT FUNCIONA
    </div>
  </React.StrictMode>
);
