import React from 'react';
import ReactDOM from 'react-dom/client';

import './index.css';

ReactDOM.createRoot(
  document.getElementById('root')!
).render(
  <React.StrictMode>
    <div
      style={{
        background: 'white',
        color: 'black',
        padding: '50px',
        fontSize: '32px',
      }}
    >
      REACT + CSS FUNCIONAN
    </div>
  </React.StrictMode>
);
